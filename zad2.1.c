#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[])
{int max;
    int z,x,c;
    printf("podaj 3 liczby :\n");
    scanf("%d %d %d\n",&z,&x,&c);
    if (z>x){max=z;
}
        else {x=max;
        }if (max>c);
        else c=max;
        printf("max to:%d",&max);
  system("PAUSE");	
  return 0;
}
